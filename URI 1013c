#include<stdio.h>
#include<stdlib.h>
int main()
{
    int a, b, c, m, MAB;
    scanf("%d%d%d", &a, &b, &c);
    m = (a + b + abs(a - b))/2;
    MAB = (m + c + abs(m - c))/2;
    printf("%d eh o maior\n", MAB);
    return 0;
}
