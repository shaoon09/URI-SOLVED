#include<stdio.h>
int main()
{
    int a, b, x, y;
    double p1, p2, ttlp;
    scanf("%d%d%lf", &a, &x, &p1);
    scanf("%d%d%lf", &b, &y, &p2);
    ttlp = (x * p1)+(y * p2);
    printf("VALOR A PAGAR: R$ %.2lf\n", ttlp);
    return 0;
}
